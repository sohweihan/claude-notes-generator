---
description: Run scoped validation and summarize the result
argument-hint: --module "CS101 - Computer Networks" --lecture "Lecture 01" --no-teaching-audit
---
Follow @docs/fix_workflow.md.

Treat the arguments below as the exact tail for `validate.py`:

`$ARGUMENTS`

Your task:
1. Run `python validate.py $ARGUMENTS`.
2. Summarize `ok`, `pending`, `review`, and `issues`.
3. Do not modify files.
